@{
    ViewData["Title"] = "Please Log In";
}

<h2>Please Log In</h2>
<p>Please <a href="@Url.Action("Login", "Account")">log in</a> or <a href="@Url.Action("Register", "Account")">register</a> before adding items to your cart.</p>
